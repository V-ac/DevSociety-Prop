
def QueenPosition(rowPosition, columnPosition):
    rowIndex = rowPosition
    columnIndex = columnPosition

    leftDiagonal = rowIndex + columnIndex
    rigthDiagonal = rowIndex + columnIndex


class QueenPosition:
    def _init_(rowIndex, columnIndex):
        row = rowIndex
        column = columnIndex

